# Payload CMS REST API (backend consumed by the Casa Maíz website).
# Discovered by inspecting the consumer site's client bundle for absolute
# fetch URLs — it is NOT the same origin as the website itself.
CMS_API_BASE_URL=https://payload-cms-poc-seven.vercel.app

# The published Casa Maíz web consumer (Next.js frontend).
CONSUMER_BASE_URL=https://payload-website-consumer.vercel.app
